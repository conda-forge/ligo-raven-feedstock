{% set name = "ligo-raven" %}
{% set version = "2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 1078466f7868a8cf5ab67ba094bc3d825413894f84b9c4e2e18d9abb8aa66ff6

build:
  number: 0
  skip: true  # [win]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - pytest-runner
    - python
    - setuptools
  run:
    - astropy
    - astropy-healpix
    - gracedb-sdk
    - healpy !=1.12.0
    - ligo.skymap >=0.1.1
    - lscsoft-glue
    - lxml
    - matplotlib-base
    - numpy >=1.14.5
    - python
    - python-lal
    - scipy >=0.7.2
    - voeventlib >=1.2

test:
  requires:
    - pip
    - pytest
  commands:
    # check requirements
    - python -m pip check
    # run test suite
    - python -m pytest -ra --pyargs ligo.tests
    # sanity check scripts
    - raven_query --help
    - raven_search --help
    - raven_skymap_overlap --help
    - raven_coinc_far --help
    - raven_calc_signif_gracedb --help

about:
  home: https://git.ligo.org/lscsoft/raven/
  dev_url: https://git.ligo.org/lscsoft/raven/
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: COPYING
  summary: Low-latency coincidence search between external triggers and GW candidates
  description: |
    Low-latency coincidence search between external triggers and GW candidates

extra:
  recipe-maintainers:
    - duncanmmacleod
